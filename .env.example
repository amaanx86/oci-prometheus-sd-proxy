# Docker Compose Environment Variables
# Copy to .env and customize for your environment
# See docs/docker-compose.md for detailed guide

# Application Settings (overrides config.yaml)
CONFIG_PATH=/etc/oci-sd/config.yaml
SERVER_PORT=8080
SERVER_TOKEN=GENERATE-WITH-openssl-rand-hex-32

DISCOVERY_TAG_KEY=monitoring
DISCOVERY_TAG_VALUE=enabled
DISCOVERY_LINUX_PORT=9100
DISCOVERY_WINDOWS_PORT=9182
DISCOVERY_REFRESH_INTERVAL=5m

# Docker Build
VERSION=dev

# Host Volume Mount Paths
# Path to config.yaml it contains OCI credentials - mounted read-only
OCI_CONFIG_PATH=./config.yaml

# Path to OCI API keys directory
# Mount from home directory: /etc/oci-sd/keys
# Or copy to local ./oci-keys directory
OCI_KEYS_PATH=/etc/oci-sd/keys

# Optional: Prometheus data directory
PROMETHEUS_DATA_PATH=./prometheus_data
